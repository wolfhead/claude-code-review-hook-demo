# Claude Code Review Hook Configuration
# This file allows you to use separate API settings for code reviews
# Copy this to .claude-review.env and uncomment the values you need
#
# These are standard Claude Code environment variables that work with
# any compatible API endpoint (Anthropic, DeepSeek, custom proxies, etc.)

# API key (use ANTHROPIC_API_KEY or ANTHROPIC_AUTH_TOKEN depending on provider)
# export ANTHROPIC_API_KEY=sk-ant-...

# API endpoint (examples below)
# Anthropic official:
# export ANTHROPIC_BASE_URL=https://api.anthropic.com
# DeepSeek:
# export ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic
# Custom proxy:
# export ANTHROPIC_BASE_URL=https://api.jiekou.ai/anthropic

# Model name (depends on your provider)
# Anthropic models:
# export ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
# export ANTHROPIC_MODEL=claude-opus-4
# DeepSeek models:
# export ANTHROPIC_MODEL=deepseek-chat

# Optional: Request timeout in milliseconds (default: 120000)
# export API_TIMEOUT_MS=600000

# Optional: Disable non-essential traffic for minimal API calls
# export CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1
